> 過去時点の記録です。最新の状態は [2026-09-12 最終修正報告](final-review-2026-09-12.md) と [テスト結果](test-results.md) を参照してください。

# ExpenseFlow 引き継ぎ資料

## この資料の目的

クラウドのChatGPT Workで作業を再開するための引き継ぎ資料です。最初にこのファイル、次に`docs/progress.md`、`docs/design.md`、`README.md`、`pom.xml`、`src/main`、`src/test`、CIを確認してください。`docs/design.md`を設計の正本とし、ユーザーの追加指示があればそちらを優先します。

## 作業対象と現在地

- リポジトリ：https://github.com/unitier0214/expense-flow
- フェーズ1レビュー基準：`c1b04d16d7d399e246a30f647200afaf9f584d67`
- 現在の対象：フェーズ5「CSV出力」まで実装・検証完了。アプリ完成範囲の最終レビュー待ち
- フェーズ2レビュー修正対象：`ff441f7a3fa39782ddbbc54719f72b6bf7f5b4d5`
- フェーズ2修正CI：[run 34394247514](https://github.com/unitier0214/expense-flow/actions/runs/34394247514)
- フェーズ3検証対象：`43dcf3791b6834f540b540a46d3349e2159af7a5`
- フェーズ3 CI：[run 34397993092](https://github.com/unitier0214/expense-flow/actions/runs/34397993092)
- フェーズ4検証対象（CSV追加前の必須機能）：`f7182a10c71e23e3839b22719aac88f1c365e91a`
- フェーズ4 CI：[run 34402776016](https://github.com/unitier0214/expense-flow/actions/runs/34402776016)
- フェーズ5 CSV実装・検証対象：`41f90066a190dc8f0f78faba1849f4b044b19722`
- フェーズ5 CI：[run 34406014385](https://github.com/unitier0214/expense-flow/actions/runs/34406014385)
- 技術前提：Java 21、Spring Boot 4.1.1、PostgreSQL 17、Testcontainers、CSRF、`open-in-view=false`

## フェーズ2で実装済み

- `/expenses`：本人の申請だけを状態・分類・利用日期間・件名で検索し、20件ページング。
- `/expenses/new` / `POST /expenses`：必須項目を検証したDRAFT作成。
- `/expenses/{id}`：本人、または同部署APPROVERがDRAFT以外の他人の申請を閲覧し、履歴を表示。
- `/expenses/{id}/edit` / `POST /expenses/{id}/edit`：DRAFT／RETURNEDだけ本人が編集。
- `POST /expenses/{id}/delete`：DRAFTだけ本人がversion付きで削除。
- `POST /expenses/{id}/submit`：DRAFT／RETURNEDから本人がversion付きで申請・再申請。
- 作成者・部署・状態・日時はDBの認証ユーザーと注入Clockから決定し、Form DTOをEntityへ直接バインドしない。
- CREATE／UPDATE／SUBMIT履歴を変更と同じトランザクションで保存。再申請もSUBMIT。
- 金額はtrim後の半角数字列だけをBigDecimalへ変換し、1〜1,000,000の整数をサービス・ドメインで検証。
- JST当日判定、Instant保存、JST表示、303の変更POST、302のSecurityログイン／ログアウトを実装。
- 古いversion・並行更新・不正遷移は409、入力は400、存在しない／権限外IDは404、予期せぬ500は照合ID付き汎用画面。

フェーズ2レビュー修正として、編集フォームのaction・キャンセル先を@PathVariable idに固定し、version型不正の表示を追加した。同時更新は独立PostgreSQLトランザクションでversion読み取りを同期し、楽観ロック競合の例外型を検証している。

## フェーズ3で追加した承認機能

- `/approvals` は同部署・本人以外・SUBMITTEDだけを`submitted_at ASC, id ASC`、20件ページングで表示する。
- 同部署の別APPROVERが承認・理由付き差戻しを実行でき、社員は403、自己処理・他部署・DRAFTは404、SUBMITTED以外の処理は409となる。
- 承認コメントは任意500文字、差戻し理由はtrim後1〜500文字。version検証、状態変更、APPROVE／RETURN履歴は同一トランザクションで処理する。
- PostgreSQLの独立トランザクションで同時承認と承認対差戻しを検証し、成功1件、楽観ロック競合1件、履歴1件を確認した。

## ロールとデモデータ

- ロールはEMPLOYEEとAPPROVERの2種類だけ。既存資料にあった「管理者」は誤記であり、管理者ロールは存在しない。
- 通常プロファイルではデモユーザーを自動生成しない。
- demoプロファイルでは営業部・開発部と架空ユーザー5名を冪等投入する。
- READMEにある`demo.employee` / `demo-password`等は、demo専用の架空公開fixtureであり、実運用の秘密情報ではない。実パスワードや`.env`はコミットしない。

## フェーズ4で追加・確認した内容

- demoプロファイルで営業部23件・開発部22件、合計45件の申請をDRAFT／SUBMITTED／RETURNED／APPROVEDへ分散投入し、CREATE／SUBMIT／RETURN／APPROVE履歴を状態と整合させた。
- デモ初期化は同名タイトルを検出すると既存行を更新せず、再実行で件数・申請内容・履歴を上書きしない。通常プロファイルでは初期化Beanを作らない。
- Compose smokeを社員の作成→編集→申請→承認者1の理由付き差戻し→本人修正・再申請→承認者2の承認→詳細・履歴→ログアウトへ拡張した。各フォームのCSRFとversionはHTMLから取得した。
- 承認待ち一覧へ状態ラベルを追加し、対象申請の件名・状態・差戻し理由・承認コメント・履歴をHTTP smokeで確認できるようにした。
- `docs/learning-guide.md`を追加し、約2ヶ月のJava学習者向けにController→Service→Repository→DB、DTO／Entity、認可、`@Transactional`、`@Version`、CSRF、検索SQL、テストとAI支援範囲を実ファイルへ結び付けて説明した。

## 検証済み範囲

GitHub ActionsのJava 21 runnerでフェーズ4対象の`./mvnw --batch-mode test`（42テスト、失敗0・エラー0・スキップ0）、`./mvnw --batch-mode verify`、`docker compose config --quiet`、拡張Compose smokeを成功させた。承認待ち、承認・差戻し、認可、競合、履歴ロールバック、45件の冪等demo投入もTestcontainers PostgreSQL 17.11とMockMvc／HTTPで確認した。詳細は`docs/test-results.md`と`docs/test-cases.md`を参照する。

ローカルではDocker CLIがなく、Mavenも依存取得先の名前解決に失敗したため、ローカルtest／verify／Compose／実ブラウザ目視は未実行。CIでの実DB・Docker・HTTP検証をスキップやモック置換で代替していない。

## フェーズ5で追加したCSV

- `GET /expenses/export.csv`を、本人一覧と同じ認可・検索条件・`updated_at DESC, id DESC`で実装した。ページングは外し、1,000件を上限とした。
- UTF-8 BOM、日本語ヘッダー・状態・JST日時、RFC 4180相当の引用、カンマ・改行・数式セル対策、固定Content-Dispositionを実装した。
- 一覧へ検索条件を保持するCSVボタンを追加し、T13とT01〜T12を回帰した。
- `CsvIntegrationTest` 7件を追加し、Phase5のCIでは合計49テスト、失敗0・エラー0・スキップ0。初回のBean重複失敗は隠さず`docs/test-results.md`へ記録した。

## 完成時点の申し送り

- Java 21、Spring Boot 4.1.1、PostgreSQL 17、Testcontainers、CSRF、`open-in-view=false`を維持している。適用済みFlyway V1は変更していない。
- フェーズ1〜5の画面・業務操作・検索・認可・楽観ロック・履歴・demoデータ・CSVを実装し、コードテストとCI smokeを通過させた。
- ローカルのJava 21／Docker／アプリ起動、実ブラウザ操作、スクリーンショットは環境制約で未実施。CIのcurl HTTP検証をブラウザ確認とは呼ばない。
- 新規デプロイ、実運用認証情報の投入、force push、履歴書き換え、設計外の機能追加は行っていない。
- Astraへ渡す最終資料は [`docs/Astra_expense-flow_status_final_2026-09-09.md`](Astra_expense-flow_status_final_2026-09-09.md)。

承認・差戻し操作の状態変更は本番Serviceだけで行い、テストfixtureはテスト側のDB操作に限定した。force push、reset、履歴書き換え、Java／Spring Bootのバージョン変更も行わないこと。Phase4の必須機能完成SHAは`f7182a10c71e23e3839b22719aac88f1c365e91a`であり、CSVはこの境界より後の独立コミットとして追加済みである。

## 最終レビュー対応（2026-09-11）

レビュー対象 [`a16b3808`](https://github.com/unitier0214/expense-flow/commit/a16b38083aed0d1b417e2c6b015743dab50e4be4) を起点に、件名依存のデモ投入判定をV2の`demo_seed_entries`へ移し、Service／Entityの空白判定を`String.strip()`へ統一した。全角スペースだけの件名・用途・差戻し理由の400と、同名・改名・削除・再初期化の回帰テストを追加した。V1は変更していない。

今回の依存検査追加対応の開始時mainは [`017f4e02`](https://github.com/unitier0214/expense-flow/commit/017f4e02729894d1b7d3f0fbe8ad0bf453e8336b) だった。旧filesystem scanのJSONは`pom.xml`だけを解析対象としていた。

CIへChromium＋Playwrightの実ブラウザ確認とスクリーンショット保存を追加し、主要申請フロー、入力エラーからの復帰、ログアウト後の保護URL拒否を実DOMで確認した。旧Trivy filesystem scanは`pom.xml`だけを解析対象としていたため、生成jarの依存検査の根拠にはしない。OWASP Dependency-CheckはNVD APIキーなしで現行APIからデータ取得できず、0件とは扱っていない。

- コード・V2・回帰テスト：[6a7c357b](https://github.com/unitier0214/expense-flow/commit/6a7c357b50582e6120bf5be581a8515b2e12e93f)、[CI 34610990791](https://github.com/unitier0214/expense-flow/actions/runs/34610990791)
- 最終検証（test 52／verify／Compose／Chromium／Trivy）：[7bb01ba1](https://github.com/unitier0214/expense-flow/commit/7bb01ba1d56f879a1d86f417b1ae222266c7a365)、[CI 34612201340](https://github.com/unitier0214/expense-flow/actions/runs/34612201340)
- 実ブラウザのスクリーンショット：[artifact](https://github.com/unitier0214/expense-flow/actions/runs/34612201340/artifacts/10268903974)
- 依存検査の修正版：[`19b6836d`](https://github.com/unitier0214/expense-flow/commit/19b6836dd000d8c605e6bb6d7986010196c7a17e)、[CI 34633201713](https://github.com/unitier0214/expense-flow/actions/runs/34633201713)。CycloneDX SBOM 105件、生成jarの`BOOT-INF/lib` 92本を記録し、Trivyの実解析対象は`target/bom.json`（`ArtifactType=cyclonedx`、`Type=jar`）。脆弱性0件。
- Trivy JSON・SBOM・依存一覧：[dependency scan artifact](https://github.com/unitier0214/expense-flow/actions/runs/34633201713/artifacts/10277196756)

ローカルのJava 21／Docker／Compose／実ブラウザ目視は未実行だが、GitHub ActionsのJava 21・PostgreSQL 17・Docker runnerでtest／verify／Compose smokeと実Chromium操作を実行済みである。V2適用前に改名・削除された旧seed申請は、V1に識別子がないため完全には推定できない。

依存検査の初回SBOM結果では`tomcat-embed-core` 11.0.24のCVE-2026-65182、CVE-2026-65905、CVE-2026-68525を検出した。Spring Boot 4.1.1を維持し、`tomcat.version`を11.0.25へ限定更新した後のrun 34633201713では0件だった。影響・修正版は[Apache Tomcat 11の公式セキュリティ情報](https://tomcat.apache.org/security-11.html)と照合している。

最終判定はAstraに委ねる。Astra確認済み・合格済みとは記載しない。

